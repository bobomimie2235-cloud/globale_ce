{% extends 'base.html.twig' %}

{% block title %}Modifier — {{ produit.intitule }}{% endblock %}

{% block body %}
<section class="auth-page">
    <div class="auth-container auth-container--lg">
        <div class="auth-card">

            <a href="{{ path('app_produit_index') }}" class="auth-back">← Retour à la liste</a>

            <h1 class="auth-titre">Modifier le produit</h1>
            <p class="auth-subtitle">{{ produit.intitule }}</p>

            {% if is_granted('ROLE_ADMIN') %}
                {{ include('produit/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}

                <hr class="auth-divider">

                <div class="auth-danger">
                    <p class="auth-danger-label">⚠️ Supprimer ce produit (action irréversible)</p>
                    {{ include('produit/_delete_form.html.twig') }}
                </div>
            {% else %}
                <p class="auth-warning">Vous n'avez pas les droits pour modifier ce produit.</p>
            {% endif %}

        </div>
    </div>
</section>
{% endblock %}
