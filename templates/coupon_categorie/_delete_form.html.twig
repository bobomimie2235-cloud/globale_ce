<form method="post" action="{{ path('app_coupon_categorie_delete', {'id': coupon_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coupon_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
