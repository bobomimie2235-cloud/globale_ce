{% extends 'base.html.twig' %}

{% block title %}CouponReduction{% endblock %}

{% block body %}
    <h1>CouponReduction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coupon_reduction.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ coupon_reduction.reference }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ coupon_reduction.intitule }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ coupon_reduction.description }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ coupon_reduction.actif ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coupon_reduction_index') }}">back to list</a>

    <a href="{{ path('app_coupon_reduction_edit', {'id': coupon_reduction.id}) }}">edit</a>

    {{ include('coupon_reduction/_delete_form.html.twig') }}
{% endblock %}
