{% extends 'base.html.twig' %}

{% block title %}Modifier CouponReduction{% endblock %}

{% block body %}
    <h1>Modifier CouponReduction</h1>

    {% if is_granted('ROLE_ADMIN') %}
        {{ include('coupon_reduction/_form.html.twig', {'button_label': 'Modifier'}) }}
    {% else %}
        <p class="alert alert-warning">Vous n'avez pas les droits pour modifier ce coupon.</p>
    {% endif %}

    <a href="{{ path('app_coupon_reduction_index') }}">Retour à la liste</a>

    {{ include('coupon_reduction/_delete_form.html.twig') }}
{% endblock %}